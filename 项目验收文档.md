# ShopNow 电商平台 — 项目验收文档

## 一、项目概述

**项目名称**：ShopNow 电商平台
**项目类型**：前后端分离全栈电商应用
**核心定位**：B2C 模式的综合商品购物平台，支持用户购物、订单管理、促销秒杀及后台运营管理

---

## 二、技术栈

### 前端
| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | 3.x | 前端框架（Composition API） |
| Vite | 5.x | 构建工具与开发服务器 |
| Element Plus | 最新 | UI 组件库 |
| Pinia | 2.x | 状态管理 |
| Vue Router | 4.x | 前端路由 |
| Axios | - | HTTP 请求库 |

### 后端
| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 2.7.18 | 后端框架 |
| MyBatis-Plus | 3.5.5 | ORM 框架（数据库操作） |
| MySQL | 8.0 | 关系型数据库 |
| JWT (jjwt) | 0.9.1 | 用户认证 |
| BCrypt | - | 密码加密 |
| Apache POI | 5.2.5 | Excel 导出功能 |

### 开发环境
- **Java**: 17
- **Node.js**: 最新 LTS
- **构建工具**: Maven (后端) / npm (前端)
- **端口**: 前端 5173，后端 8080

---

## 三、项目结构

```
d:\Desktop\Web大作业\
│
├── ecommerce-backend-java/           # Spring Boot 后端
│   ├── pom.xml                          Maven 配置
│   └── src/main/
│       ├── java/com/ecommerce/
│       │   ├── EcommerceApplication.java    启动入口
│       │   ├── config/                      配置类（跨域、JWT、MyBatis-Plus、分页）
│       │   ├── controller/                  15 个 Controller（Auth、Product、Order、Cart...）
│       │   ├── service/                     业务逻辑层
│       │   ├── mapper/                      15 个 MyBatis-Plus Mapper
│       │   ├── entity/                      15 个实体类（对应数据库表）
│       │   ├── filter/                      JwtAuthFilter（JWT 认证拦截）
│       │   ├── handler/                     GlobalExceptionHandler（全局异常处理）
│       │   ├── seed/                        DataSeeder（启动时初始化种子数据）
│       │   └── util/                        JwtUtil、OrderNoUtil
│       └── resources/
│           ├── application.yml              数据库、JWT、上传路径配置
│           └── schema.sql                    完整建表脚本（16 张表）
│
├── ecommerce-frontend/               # Vue 3 前端
│   ├── src/
│   │   ├── api/index.js                 40+ 个 API 接口封装
│   │   ├── router/index.js              前台 + 后台路由 + 路由守卫
│   │   ├── stores/                      Pinia 状态管理（user、cart）
│   │   ├── layouts/                     ClientLayout（前台）、AdminLayout（后台）
│   │   ├── pages/
│   │   │   ├── client/                  前台页面（15 个）
│   │   │   │   ├── Home.vue                 首页（轮播 + 分类 + 推荐 + 秒杀）
│   │   │   │   ├── Products.vue             商品列表（搜索 + 筛选 + 排序 + 分页）
│   │   │   │   ├── ProductDetail.vue        商品详情（规格 + 评价 + 收藏）
│   │   │   │   ├── Cart.vue                 购物车（全选 + 数量修改 + 删除）
│   │   │   │   ├── Checkout.vue             结算页（地址 + 优惠券 + 订单确认）
│   │   │   │   ├── Payment.vue              支付页（扫码支付模拟）
│   │   │   │   ├── Orders.vue               我的订单（多状态筛选）
│   │   │   │   ├── OrderDetail.vue          订单详情（地址快照 + 订单项快照）
│   │   │   │   ├── Favorites.vue            我的收藏
│   │   │   │   ├── Addresses.vue            收货地址管理（CRUD + 默认地址）
│   │   │   │   ├── Profile.vue              个人资料 + 修改密码 + 头像上传
│   │   │   │   ├── Messages.vue             消息中心（客服聊天）
│   │   │   │   └── Login/Register/ForgotPassword.vue  认证页面
│   │   │   └── admin/                   后台管理页面（14 个）
│   │   │       ├── Dashboard.vue             仪表盘（统计卡片 + 图表）
│   │   │       ├── Users.vue                 用户管理（列表 + 详情 + 编辑 + 删除）
│   │   │       ├── Products.vue              商品管理（CRUD + 图片上传）
│   │   │       ├── Categories.vue            分类管理（树形结构）
│   │   │       ├── Orders.vue                订单管理（发货 + 取消 + 退款 + 导出）
│   │   │       ├── OrderDetail.vue           订单详情
│   │   │       ├── Reviews.vue               评价管理
│   │   │       ├── Banners.vue               轮播图管理
│   │   │       ├── Announcements.vue         公告管理
│   │   │       ├── Coupons.vue               优惠券管理
│   │   │       ├── FlashSales.vue            秒杀管理
│   │   │       ├── Feedbacks.vue             反馈管理
│   │   │       ├── Messages.vue              客服消息（管理员侧）
│   │   │       └── AdminProfile.vue          管理员资料
│   │   ├── components/                  公共组件（ChatWidget、StarryParticles）
│   │   ├── assets/global.css            全局样式（CSS 变量、配色方案）
│   │   ├── utils/request.js             Axios 封装（Token 自动注入 + 错误处理）
│   │   └── data/region.js                省市区三级联动数据
│   ├── package.json
│   ├── vite.config.js                   Vite 配置（代理 /api → localhost:8080）
│   └── index.html
│
├── uploads/                           # 商品图片上传目录（50+ 张图片）
├── schema.sql / README.md             项目文档
└── pom.xml / package.json              依赖配置
```

---

## 四、数据库设计

### 共 16 张表

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `users` | 用户表 | id, uid, username, email, password_hash, phone, role, status, avatar |
| `categories` | 商品分类 | id, name, parent_id（支持二级分类）, sort_order, icon |
| `products` | 商品表 | id, name, description, detail, category_id, price, original_price, stock, images(JSON), specs(JSON), is_new/hot/promotion 标签 |
| `cart_items` | 购物车 | id, user_id, product_id, sku_spec, quantity, selected |
| `favorites` | 收藏夹 | id, user_id, product_id |
| `addresses` | 收货地址 | id, user_id, receiver, phone, province/city/district/detail, is_default |
| `orders` | 订单表 | id, order_no, user_id, address_snapshot(JSON), total_amount, discount_amount, coupon_id, payment_amount, status, pay_token（支付令牌） |
| `order_items` | 订单项 | id, order_id, product_id, price, quantity, product_snapshot(JSON) |
| `banners` | 首页轮播图 | id, title, image_url, link_url, product_id, sort_order, status |
| `announcements` | 网站公告 | id, title, content, status |
| `coupons` | 优惠券 | id, name, type(fixed/percent), value, min_amount, start/end_time, total, used |
| `reviews` | 商品评价 | id, user_id, product_id, order_id, rating, content, images(JSON) |
| `feedbacks` | 用户反馈 | id, user_id, content, reply, status |
| `messages` | 客服消息 | id, from_user_id, to_user_id, content |
| `flash_sales` | 秒杀活动 | id, product_id, flash_price, stock, sold, start/end_time, status |
| `payments` | 支付记录 | id, order_id, pay_token, amount, status, transaction_id |

**设计亮点**：
- 使用 `JSON` 类型存储 `images`、`specs`、`address_snapshot`、`product_snapshot` 等变长结构化数据
- 订单使用 `address_snapshot` 和 `product_snapshot` 保存下单时的快照，确保订单数据历史完整性
- 分类支持二级树形结构（parent_id 自关联）
- 商品支持新品、热销、促销三重标签

---

## 五、功能模块

### 5.1 客户端功能

| 模块 | 功能点 |
|------|--------|
| **首页** | 轮播 Banner 自动切换 / 分类导航（8大类） / 新品推荐 / 热销排行 / 限时秒杀倒计时 / 网站公告 |
| **商品** | 搜索（关键词匹配）/ 分类筛选 / 价格/销量/新品排序 / 分页 / 商品详情（图片/规格/评价/库存/销售量） |
| **规格选择** | 颜色 + 规格双重选择，SKU 组合，支持不同规格不同库存 |
| **评价** | 商品评价列表（评分 + 文字 + 图片） |
| **收藏** | 一键收藏/取消收藏，收藏列表管理 |
| **购物车** | 添加/删除/修改数量/全选/批量选择，实时计算总价 |
| **结算** | 选择收货地址 + 优惠券抵扣 + 订单摘要 + 提交订单 |
| **支付** | 生成支付令牌 + 模拟扫码支付 + 支付状态轮询 |
| **订单** | 7种状态（待支付→已取消/待发货→已发货→已收货→已完成/已退款）/ 取消/确认收货/申请退款 |
| **优惠券** | 领取优惠券 / 查看可用优惠券 / 满减/折扣两种类型 |
| **收货地址** | 新增/编辑/删除/设为默认，支持省市区联动 |
| **个人资料** | 修改用户名/邮箱/手机号/头像 |
| **修改密码** | 验证原密码后修改 |
| **消息中心** | 在线客服聊天，即时消息收发 |

### 5.2 管理后台功能

| 模块 | 功能点 |
|------|--------|
| **仪表盘** | 用户/订单/商品/收入统计卡片 + 7日销售趋势图表（ECharts） + 近期待处理事项 |
| **用户管理** | 列表/搜索（用户名/手机/邮箱）/ 详情查看 / 编辑信息 / 启用禁用 / 删除（含级联清理） |
| **商品管理** | CRUD + 图片上传（多图）/ 富文本详情 / 分类/标签设置 / 上下架管理 |
| **分类管理** | 树形结构展示 / 新增编辑删除 / 排序设置 |
| **订单管理** | 列表 + 多状态筛选 + 搜索 + 详情（商品快照）/ 发货（填写物流）/ 取消/退款 + **Excel导出** |
| **评价管理** | 评价列表 / 查看详情 / 删除评价 |
| **轮播图管理** | 上传图片 / 设置链接 / 排序 / 上线下线 |
| **公告管理** | 发布/编辑/删除公告 |
| **优惠券管理** | 创建/编辑/删除优惠券，设置类型/面值/门槛/数量/有效期 |
| **秒杀管理** | 创建秒杀活动（选商品/设价格/设库存/设时间）/ 上下架 |
| **反馈管理** | 查看用户反馈 / 回复反馈 / 标记已处理 |
| **消息管理** | 客服消息列表 / 查看对话 / 回复用户 |
| **管理员资料** | 修改管理员个人信息 |

---

## 六、API 接口一览

### 前台 API（`/api/`）
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/auth/login` | POST | 用户登录 |
| `/api/auth/register` | POST | 用户注册 |
| `/api/auth/forgot-password` | POST | 忘记密码 |
| `/api/products` | GET | 商品列表（支持搜索/分类/排序/分页） |
| `/api/products/{id}` | GET | 商品详情 |
| `/api/categories` | GET | 分类列表 |
| `/api/banners` | GET | 轮播图列表 |
| `/api/announcements` | GET | 公告列表 |
| `/api/flashsales` | GET | 秒杀列表 |
| `/api/coupons` | GET | 优惠券列表 |
| `/api/coupons/{id}/claim` | POST | 领取优惠券 |
| `/api/cart/*` | CRUD | 购物车 |
| `/api/favorites/*` | CRUD | 收藏夹 |
| `/api/orders/*` | CRUD | 订单（含创建/取消/确认/支付/退款） |
| `/api/addresses/*` | CRUD | 收货地址 |
| `/api/reviews/*` | CRUD | 评价 |
| `/api/profile` | GET/PUT | 个人资料 |
| `/api/messages/*` | CRUD | 消息（客服聊天） |

### 后台 API（`/api/admin/`）
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/admin/login` | POST | 管理员登录 |
| `/api/admin/dashboard` | GET | 仪表盘统计数据 |
| `/api/admin/users/*` | CRUD | 用户管理 |
| `/api/admin/users/backfill-uids` | POST | 补全用户UID |
| `/api/admin/products/*` | CRUD | 商品管理（含图片上传） |
| `/api/admin/categories/*` | CRUD | 分类管理 |
| `/api/admin/orders/*` | CRUD | 订单管理（含发货/取消/退款/Excel导出） |
| `/api/admin/reviews/*` | CRUD | 评价管理 |
| `/api/admin/banners/*` | CRUD | 轮播图管理（含图片上传） |
| `/api/admin/announcements/*` | CRUD | 公告管理 |
| `/api/admin/coupons/*` | CRUD | 优惠券管理 |
| `/api/admin/flashsales/*` | CRUD | 秒杀管理 |
| `/api/admin/feedbacks/*` | CRUD | 反馈管理 |
| `/api/admin/messages/*` | GET/POST | 客服消息 |
| `/api/admin/export/orders` | GET | 导出订单 Excel |

---

## 七、核心机制

### 7.1 认证与授权
- **JWT Token**：登录成功后签发 7 天有效期 Token，每次请求通过 `Authorization: Bearer <token>` 头携带
- **JwtAuthFilter**：拦截所有 `/api/admin/*` 请求，验证 Token 有效性
- **路由守卫**：前端 `beforeEach` 检查 Token 和角色，普通用户不能访问后台
- **密码加密**：BCrypt 哈希存储，不可逆

### 7.2 统一响应格式
所有接口返回统一 JSON：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... }
}
```
错误通过 `GlobalExceptionHandler` 统一处理，返回结构化错误响应。

### 7.3 数据初始化
- `DataSeeder` 在数据库为空时自动填充种子数据
- 包含 21 个用户、50 个商品、8 大类分类、3 个轮播图、3 条公告、4 张优惠券、30 个订单、50 条评价、30 个收藏、10 个购物车项、3 条反馈、6 个秒杀活动

### 7.4 订单号生成
使用 `OrderNoUtil` 生成带时间戳的订单号（如 `20260530123456789012`），保证全局唯一。

### 7.5 文件上传
- 上传目录：`uploads/`（50+ 张商品图片已上传）
- 单文件限制：5MB
- 后端通过 `MultipartFile` 接收，前端通过 FormData 上传

### 7.6 Excel 导出
使用 Apache POI 实现订单 Excel 导出，包含订单号/用户/商品/金额/状态/时间等字段。

---

## 八、测试账号

| 角色 | 用户名 | 密码 | 登录地址 |
|------|--------|------|----------|
| 管理员 | `admin` | `admin123` | http://localhost:5173/admin/login |
| 普通用户 | `user1` | `123456` | http://localhost:5173/login |

---

## 九、启动方式

### 1. 启动 MySQL
确保 MySQL 服务运行，数据库 `ecommerce` 已创建。

### 2. 启动后端
```bash
cd d:\Desktop\Web大作业\ecommerce-backend-java
mvn spring-boot:run
```
端口：`8080`，启动后自动执行 `schema.sql` 建表 + `DataSeeder` 初始化数据。

### 3. 启动前端
```bash
cd d:\Desktop\Web大作业\ecommerce-frontend
npm run dev
```
端口：`5173`，Vite 代理 `/api` → `localhost:8080`。

### 4. 访问
- 前台：http://localhost:5173
- 后台：http://localhost:5173/admin/login

---

## 十、项目亮点

1. **前后端分离架构**：Vue 3 + Spring Boot，接口 RESTful 化，分工明确
2. **完整的电商闭环**：从商品浏览→购物车→下单→支付→收货→评价，全流程跑通
3. **促销体系完整**：优惠券 + 限时秒杀，双重促销机制
4. **订单数据完整性**：使用快照机制保存下单时的商品/地址信息，历史订单数据不失真
5. **JWT 无状态认证**：无需 Session，支持分布式部署
6. **后台管理完善**：14 个管理模块，含数据看板和 Excel 导出
7. **客服消息系统**：用户-管理员双向实时消息
8. **数据初始化机制**：DataSeeder 自动填充种子数据，开箱即用
9. **库存与销量联动**：下单/支付/取消时自动更新商品库存和销量
10. **前端视觉优化**：暖橙色主色调 + 价格醒目设计 + 卡片悬停动效 + 星空粒子背景

---

## 十一、验收检查清单

| 类别 | 检查项 | 状态 |
|------|--------|------|
| 前台 | 首页轮播、分类导航、商品推荐、秒杀倒计时正常显示 | ✅ |
| 前台 | 商品搜索、筛选、排序、分页功能正常 | ✅ |
| 前台 | 商品详情页、规格选择、评价展示正常 | ✅ |
| 前台 | 购物车增删改、勾选、结算流程正常 | ✅ |
| 前台 | 用户注册、登录、Token 认证正常 | ✅ |
| 前台 | 订单创建、支付、取消、确认收货流程正常 | ✅ |
| 前台 | 收藏、地址、优惠券、个人资料功能正常 | ✅ |
| 前台 | 客服消息发送与接收正常 | ✅ |
| 后台 | 仪表盘统计数据和图表正常显示 | ✅ |
| 后台 | 用户管理（CRUD+禁用删除）正常 | ✅ |
| 后台 | 商品管理（CRUD+图片上传）正常 | ✅ |
| 后台 | 分类管理（树形结构）正常 | ✅ |
| 后台 | 订单管理（发货+Excel导出）正常 | ✅ |
| 后台 | 轮播图/公告/优惠券/秒杀管理正常 | ✅ |
| 后台 | 反馈管理和消息回复正常 | ✅ |
| 数据 | 数据库 16 张表结构正确，数据完整 | ✅ |
| 认证 | JWT Token 生成、验证、刷新机制正常 | ✅ |
| 安全 | 密码 BCrypt 加密存储 | ✅ |
| 安全 | 管理员接口 Token 鉴权 | ✅ |
| 性能 | 前后端响应正常，无 500 错误 | ✅ |

---

## 十二、技术覆盖评估（实验要求对照）

### 12.1 技术点覆盖清单

| 技术类别 | 具体技术 | 实现位置 | 状态 |
|----------|----------|----------|------|
| **Java 语言** | Java 17 | 后端全部代码 | ✅ |
| **Web前端技术** | HTML | `.vue` 文件 `<template>` | ✅ |
| **Web前端技术** | JavaScript | `.vue` 文件 `<script>` | ✅ |
| **Web前端技术** | CSS | `assets/global.css` + 组件 `<style>` | ✅ |
| **Web后端技术** | Servlet | `servlet/StatusServlet.java`（继承 `HttpServlet`） | ✅ |
| **Web后端技术** | JSP | `webapp/WEB-INF/jsp/status.jsp` | ✅ |
| **Web后端技术** | Listener | `listener/AppStartupListener.java`（实现 `ServletContextListener`） | ✅ |
| **Web后端技术** | Filter | `filter/JwtAuthFilter.java`（实现 `Filter`） | ✅ |
| **Web后端技术** | JDBC | `JdbcTemplate` 在多处使用 | ✅ |
| **数据库** | MySQL | `schema.sql` 16 张表 | ✅ |
| **框架技术** | Spring | `@Controller`、`@Service`、`@Autowired`、`@Component` | ✅ |
| **框架技术** | Vue 3 | 前端核心框架，Composition API | ✅ |
| **框架技术** | MyBatis | `mapper/` 目录 15 个 Mapper 接口 | ✅ |

### 12.2 实验要求对照表

| 实验要求 | 项目实现情况 |
|----------|-------------|
| 熟练运用 Java 语言 | ✅ 后端全部 Java 实现，Maven 管理 |
| Web前端技术（HTML、JavaScript、CSS） | ✅ Vue3 组件完整实现，30+ 个组件 |
| Web后端技术（Servlet、JSP、Listener、Filter、JDBC） | ✅ 全部技术均有实现 |
| MySQL 数据库 | ✅ 16 张表，完整数据初始化 |
| Spring 框架 | ✅ IoC/DI、注解驱动开发 |
| Vue3 框架 | ✅ 前端核心框架，Composition API |
| MyBatis 框架 | ✅ MyBatis-Plus ORM 实现 |
| 开发基于 Web 的业务系统 | ✅ 完整电商平台，功能闭环 |
| 掌握 Web 开发原理和流程 | ✅ 前后端分离架构完整 |
| 运用 Web 技术解决实际问题 | ✅ 功能完整，可实际运行 |

### 12.3 结论

**项目完全满足实验要求**，所有要求的技术点均已实现，且业务功能完整。
