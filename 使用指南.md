# ShopNow 电商平台 — Docker 部署指南

## 环境要求

- [Docker Desktop](https://www.docker.com/products/docker-desktop/)（Windows/Mac）或 Docker Engine（Linux）

## 快速启动

```bash
# 1. 进入项目目录
cd ShopNow-ecommerce

# 2. 启动所有服务
docker compose up -d
```

首次启动需下载镜像并构建，约 3~5 分钟。完成后访问：

| 地址 | 页面 |
|------|------|
| http://localhost | 前台商城 |
| http://localhost/admin | 后台管理 |
| http://localhost:8080 | 后端 API |

## 配置（可选）

如需修改数据库密码，复制 `.env.example` 为 `.env` 并编辑：

```bash
cp .env.example .env
# 编辑 .env 中的密码
```

## 默认账户

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | admin123 |
| 普通用户 | user1 | user123 |

## 常用命令

```bash
docker compose up -d          # 启动
docker compose down           # 停止
docker compose logs -f        # 查看日志
docker compose restart        # 重启
```
