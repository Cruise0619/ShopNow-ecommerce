# ShopNow 电商平台 — Docker 部署指南

## 前置条件

1. 安装 **Docker Desktop**（[下载链接](https://www.docker.com/products/docker-desktop/)）
   - Windows：安装后需重启电脑，确保 Docker 图标显示为绿色运行状态
   - Mac：直接安装拖入 Applications
   - Linux：`curl -fsSL https://get.docker.com | sh`
2. 安装 **Git**（[下载链接](https://git-scm.com/downloads)）

验证安装：

```bash
docker --version    # 应输出 Docker version 24.x 或更高
git --version       # 应输出 git version 2.x 或更高
```

## 部署步骤

### 1. 克隆项目

```bash
git clone https://github.com/Cruise0619/ShopNow-ecommerce.git
cd ShopNow-ecommerce
```

### 2. 一键启动

```bash
docker compose up -d
```

首次启动会自动完成：
- 下载 MySQL 8.0、Java 17、Nginx 等基础镜像
- 编译后端 Java 项目（Maven）
- 编译前端 Vue3 项目（npm + Vite）
- 自动创建 16 张数据库表
- 自动写入种子数据（商品、用户、优惠券、订单等）

耗时约 **3~5 分钟**，取决于网络和电脑性能。再次启动只需几秒。

### 3. 确认运行状态

```bash
docker compose ps
```

三个服务均为 `Up`（healthy）状态即部署成功：

```
NAME               STATUS
shopnow-mysql      Up (healthy)
shopnow-backend    Up (healthy)
shopnow-frontend   Up
```

### 4. 访问系统

| 地址 | 页面 | 说明 |
|------|------|------|
| http://localhost | 前台商城 | 浏览商品、下单、咨询客服 |
| http://localhost/admin | 后台管理 | 管理商品、订单、用户 |
| http://localhost:8080/api/products | 后端 API | 查看接口数据 |

## 默认账户

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | admin | admin123 | 后台管理全部权限 |
| 普通用户 | user1 | 123456 | 已有收货地址和订单数据 |
| 普通用户 | user2 | 123456 | 已有收藏和购物车数据 |
| ... | user3 ~ user20 | 123456 | 共 20 个测试用户 |

## 功能模块

| 模块 | 前台 | 后台 |
|------|------|------|
| 商品浏览 | 首页轮播 + 分类 + 搜索 + 筛选 | 商品 CRUD + 图片管理 + 标签 |
| 购物车 | 加入购物车 + 数量修改 + 全选 | — |
| 收藏夹 | 收藏/取消 + 列表查看 | — |
| 下单支付 | 创建订单 + 二维码支付 + 模拟付款 | 订单管理 + 状态流转 |
| 收货地址 | 新增/删除/设为默认 | — |
| 优惠券 | 领券中心 + 下单使用 | 创建/启用/禁用优惠券 |
| 限时秒杀 | 秒杀商品展示 | 设置秒杀活动 |
| 客服消息 | 用户端实时聊天 | 客服工作台 + 快捷回复 |
| 评价反馈 | 发表评论 + 提交反馈 | 评论审核 + 反馈回复 |
| 用户管理 | 注册/登录/个人中心 | 用户列表 + 封禁/启用 |
| 轮播图 | 自动轮播 | 配置轮播内容 |
| 公告 | 公告列表 | 发布/下架公告 |

## 常用命令

```bash
docker compose up -d              # 启动所有服务
docker compose down               # 停止并删除容器
docker compose down -v            # 停止并删除容器 + 数据卷（重置数据库）
docker compose restart            # 重启所有服务
docker compose logs -f            # 实时查看所有日志
docker compose logs -f backend    # 只查看后端日志
docker compose ps                 # 查看服务状态
docker compose build --no-cache   # 强制重新构建镜像
```

## 故障排查

### 端口被占用

```bash
# Windows 查看端口占用
netstat -ano | findstr ":80"
netstat -ano | findstr ":8080"
netstat -ano | findstr ":3307"

# 停止占用进程（替换 PID）
taskkill /PID <PID> /F
```

### 数据库连接失败

确认 MySQL 容器已启动且健康：

```bash
docker compose logs mysql
```

### 想清空数据重新开始

```bash
docker compose down -v    # 删除数据卷
docker compose up -d      # 重新启动，自动重建数据库
```

### 前端页面 404

Nginx SPA 路由已配置，非静态文件请求自动回退到 `/index.html`。如仍有问题：

```bash
docker compose restart frontend
```

## 项目架构

```
浏览器 (localhost:80)
    │
    ▼
┌──────────────────┐
│  Nginx (前端)     │  静态文件 + SPA 路由
│  port: 80        │  /api/ → 代理到后端
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  Spring Boot     │  REST API + JWT 认证
│  port: 8080      │  MyBatis-Plus + BCrypt
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  MySQL 8.0       │  自动建表 + 数据填充
│  port: 3306      │  16 张表
└──────────────────┘
```
