# ShopNow 电商平台 — 启动指南

## 目录

1. [你需要安装的软件](#一你需要安装的软件)
2. [第一步：启动 MySQL 数据库](#二第一步启动-mysql-数据库)
3. [第二步：创建数据库（仅首次）](#三第二步创建数据库仅首次)
4. [第三步：启动后端](#四第三步启动后端)
5. [第四步：启动前端](#五第四步启动前端)
6. [第五步：打开网站](#六第五步打开网站)
7. [测试账号](#七测试账号)
8. [常见问题](#八常见问题)

---

## 一、你需要安装的软件

在开始之前，请确认你的电脑已经安装了以下软件：

| 软件 | 作用 | 怎么检查是否已安装 |
|------|------|-------------------|
| **Java** | 运行后端程序 | 打开命令提示符(CMD)，输入 `java -version`，看到版本号说明已安装 |
| **Maven** | 编译后端项目 | 打开命令提示符(CMD)，输入 `mvn -v`，看到版本号说明已安装 |
| **MySQL** | 存储数据 | 在 Windows 搜索栏搜"服务"，找到 MySQL 服务 |
| **Node.js** | 运行前端项目 | 打开命令提示符(CMD)，输入 `node -v`，看到版本号说明已安装 |

> 如果某个软件没安装，请先下载安装：
> - Java: https://www.oracle.com/java/technologies/downloads/ （下载 JDK 17）
> - Maven: https://maven.apache.org/download.cgi （下载 Binary zip，解压到 C:\Program Files）
> - MySQL: https://dev.mysql.com/downloads/installer/
> - Node.js: https://nodejs.org/ （下载 LTS 版本）

---

## 二、第一步：启动 MySQL 数据库

**MySQL 必须处于"正在运行"状态，后端才能启动！**

### 操作方法：

1. 按键盘 `Win + R`，输入 `services.msc`，按回车
2. 在服务列表中找到 **MySQL**（可能是 MySQL80 或 MySQL57）
3. 右键点击 → **启动**
4. 确认"状态"列显示为 **"正在运行"**

![示意图：服务窗口中找到 MySQL，状态为"正在运行"]

> 如果找不到 MySQL 服务，说明 MySQL 没安装，请先安装 MySQL。

---

## 三、第二步：创建数据库（仅首次）

**这一步骤只需要做一次！** 如果之前已经创建过 `ecommerce` 数据库，可以跳过。

### 操作方法：

1. 打开 **MySQL 命令行客户端**（开始菜单搜 MySQL → MySQL Command Line Client）
2. 输入你的 MySQL 密码（安装 MySQL 时设置的 root 密码）并回车
3. 输入以下命令并回车：
   ```sql
   CREATE DATABASE IF NOT EXISTS ecommerce DEFAULT CHARSET utf8mb4;
   ```
4. 看到 `Query OK` 说明创建成功
5. 输入 `exit` 退出

> 如果你没有 MySQL 命令行客户端，也可以使用 Navicat、DBeaver 等图形化工具创建数据库。

---

## 四、第三步：启动后端

### 方法 A：双击启动（推荐）

直接双击项目文件夹中的 **`启动后端.bat`** 文件。

一个黑色命令窗口会打开，首次启动会自动下载依赖（约 3-5 分钟），之后显示：

```
Started EcommerceApplication in X.XXX seconds
```

看到这行字就说明后端启动成功了！

### 方法 B：命令行启动

1. 按 `Win + R`，输入 `cmd`，按回车
2. 输入以下命令：
   ```bash
   cd /d "D:\Desktop\Web大作业\ecommerce-backend-java"
   mvn spring-boot:run
   ```

### 启动成功的标志

看到类似以下内容，说明启动成功：

```
Tomcat started on port(s): 8080 (http)
Started EcommerceApplication in 1.936 seconds
[Seeder] Data already exists, skipping seed.
```

> **首次启动会自动创建表结构和测试数据。**
>
> 如果看到 `[Seeder] Seeding database...` 说明正在写入初始数据。
>
> 如果看到 `[Seeder] Data already exists, skipping seed.` 说明数据已经存在，直接跳过。

---

## 五、第四步：启动前端

### 方法 A：双击启动（推荐）

直接双击项目文件夹中的 **`启动前端.bat`** 文件。

### 方法 B：命令行启动

1. 再打开一个新的命令提示符窗口（不要关闭后端窗口！）
2. 输入以下命令：
   ```bash
   cd /d "D:\Desktop\Web大作业\ecommerce-frontend"
   npm run dev
   ```

### 启动成功的标志

看到类似以下内容，说明启动成功：

```
VITE v5.4.7  ready in xxx ms
➜  Local:   http://localhost:5173/
```

---

## 六、第五步：打开网站

打开浏览器，访问以下地址：

| 页面 | 地址 |
|------|------|
| **商城首页** | http://localhost:5173 |
| **用户登录** | http://localhost:5173/login |
| **用户注册** | http://localhost:5173/register |
| **管理后台** | http://localhost:5173/admin/login |

---

## 七、测试账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | `admin` | `admin123` | 可以访问管理后台 |
| 普通用户 | `user1` | `123456` | 可以浏览商品、下单购物 |

---

## 八、常见问题

### ❌ 问题1：双击 bat 文件闪退

**原因**：Maven 或 Node.js 没安装，或者没配置环境变量。

**解决**：
1. 打开命令提示符 (CMD)
2. 输入 `java -version`，检查 Java 是否安装
3. 输入 `mvn -v`，检查 Maven 是否安装
4. 如果提示"不是内部或外部命令"，说明环境变量没配好
5. 使用**方法 B** 手动 cd 到项目目录再执行命令，能看到具体报错信息

---

### ❌ 问题2：后端启动报 "Connection refused"

**完整报错**：
```
Communications link failure
```

**原因**：MySQL 没启动。

**解决**：
1. 按 `Win + R`，输入 `services.msc`
2. 找到 MySQL 服务 → 右键 → 启动
3. 重新启动后端

---

### ❌ 问题3：后端启动报 "Access denied for user"

**完整报错**：
```
Access denied for user 'root'@'localhost'
```

**原因**：MySQL 密码不对。

**解决**：
1. 用文本编辑器打开 `ecommerce-backend-java\src\main\resources\application.yml`
2. 找到 `password: xuanxuan0513` 这行
3. 改成你自己的 MySQL root 密码
4. 保存文件，重新启动后端

---

### ❌ 问题4：后端启动报 "Unknown database 'ecommerce'"

**原因**：没创建数据库。

**解决**：按照[第二步](#三第二步创建数据库仅首次)创建 `ecommerce` 数据库。

---

### ❌ 问题5：打开网站是空白页

**原因**：前端没启动，或者后端没启动。

**解决**：
1. 确认后端窗口显示 `Started EcommerceApplication`
2. 确认前端窗口显示 `Local: http://localhost:5173/`
3. 两个窗口都要开着，不要关掉任何一个！

---

### ❌ 问题6：端口被占用

**完整报错**：
```
Port 8080 was already in use
```

**原因**：上次启动的后端没关，端口还被占着。

**解决**：
1. 打开命令提示符，输入：
   ```bash
   netstat -ano | findstr 8080
   ```
2. 记下最后一列的数字（PID）
3. 输入：`taskkill /PID 那个数字 /F`
4. 重新启动后端

---

### ❌ 问题7：前端启动报 "npm 不是内部或外部命令"

**原因**：Node.js 没安装。

**解决**：去 https://nodejs.org 下载安装 Node.js（选择 LTS 版本）。

---

> 如果以上步骤都没解决你的问题，请截图报错信息并询问。
